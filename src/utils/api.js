// API Utility handles all network requests
export const fetchRoutes = async (params) => {
    console.log("Fetching routes with params:", params);
    // Placeholder for actual API call
    return [];
};
